---
import { Icon } from "leaflet";
export const iconRed = new Icon({
  iconUrl: "/leaflet/marker-red.svg",
  iconRetinaUrl: "/leaflet/marker-red.svg",
  iconSize: [26, 42],
  iconAnchor: [13, 42],
  popupAnchor: [-3, -76],
  shadowUrl: "/leaflet/marker-shadow.png",
  shadowRetinaUrl: "/leaflet/marker-shadow.png",
  shadowSize: [68, 50],
  shadowAnchor: [22, 49],
});

import "leaflet.locatecontrol"; // Import plugin
// Añadir el control de localización al mapa usando control.locate con opciones
export const locateOptions: L.Control.LocateOptions = {
  // setView: "once",
  keepCurrentZoomLevel: true,
  drawCircle: false,
  drawMarker: false,
  showPopup: false,
  strings: {
    title: "Muestrame donde estoy",
    metersUnit: "meters",
    feetUnit: "feet",
    popup: "You are within {distance} {unit} from this point",
    outsideMapBoundsMsg: "You seem to be outside the boundaries of the map",
  },
};
---
